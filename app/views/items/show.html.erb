<div class="container-fluid">
    <div class="row">
      <div class="col-md text-center">
        <%= image_tag @item.image_url %>
      </div>
      <div class="col-md">
        <h1><%= @item.title %></h1>
        <p><%= @item.description %></p>
        <div class="row">
          <div class="col-md">
            <h3 class="float-right"><%= @item.price %> €</h3>
          </div>
          <div class="col-md">
              <a href="#" class="btn btn-secondary"> Ajouter au panier</a>
          </div>
        </div>
      </div>
    </div>
  </div>